# The browser reaches the API through a same-origin rewrite so the session
# cookies stay first-party. Keep this relative.
NEXT_PUBLIC_API_URL=/api
# Destination of the /api/:path* rewrite: the public URL of the API.
API_URL=
# Absolute API URL used by the realtime WebSocket, which cannot be proxied.
NEXT_PUBLIC_WS_URL=
